# Quarto working files
/.quarto/
/*_files/

# The rendered complete worksheet IS committed on purpose, so people can read
# it without running R. The teaching version can't render (it has ____ blanks).
/mapping-in-r-workshop-teaching.html

# R session junk
Rplots.pdf
.Rhistory
.RData
.Rproj.user/

# macOS / Windows
.DS_Store
Thumbs.db

# Saved map output from the ggsave() chunk
shadiest_spot.png
campus_tree_map.png
